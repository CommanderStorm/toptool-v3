Hallo,

hier ist das aktuelle Protokoll für {{ meeting.title }} vom {{ meeting.time | date:"SHORT_DATE_FORMAT" }}. Die PDF-Version gibt es unter {{ pdf_url }}, die HTML-Version unter {{ html_url }}. Die Text-Version gibt es unten.

Kommentare, Verbesserungsvorschläge bitte direkt an den Protokollanten {{ protokollant.get_full_name }} ({{ protokollant.email }}) schicken.

Viele Grüße,
Das Sitzungstool
